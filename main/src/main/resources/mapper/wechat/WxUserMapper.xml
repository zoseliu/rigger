<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.northsoft.modules.wechat.dao.WxUserDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.northsoft.modules.wechat.entity.WxUserEntity">
        <id column="user_id" property="user_id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="mobile" property="mobile"/>
        <result column="status" property="status"/>
        <result column="create_time" property="create_time"/>
        <result column="openid" property="openid"/>
    </resultMap>

    <!-- 根据key，查询value -->
    <select id="queryByOpenId" parameterType="string" resultType="com.northsoft.modules.wechat.entity.WxUserEntity">
        select * from wx_user where openid = #{openxid}
    </select>

</mapper>
